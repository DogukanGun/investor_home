# Frontend environment variables

# API base URL - can be relative (same domain) or absolute (different domain)
# For production: https://investor.dogukangundogan.com
# For development: http://localhost:8000 (or empty for relative URLs with Vite proxy)
VITE_API_BASE_URL=
